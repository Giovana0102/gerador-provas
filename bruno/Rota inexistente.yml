info:
  name: Rota inexistente
  type: http
  seq: 5

http:
  method: GET
  url: {{baseUrl}}/rota-inexistente

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Testa o comportamento da API ao acessar uma rota inexistente.