info:
  name: Get Users
  type: http
  seq: 1

http:
  method: GET
  url: {{baseUrl}}/users

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Lista todos os usuários cadastrados.